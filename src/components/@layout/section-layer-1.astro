---
import type { ImageMetadata } from "astro";

interface Props {
  desktopImage: ImageMetadata;
  mobileImage: ImageMetadata;
  hasGap?: boolean;
}

const { desktopImage, mobileImage, hasGap = true } = Astro.props;
---

<section
  class="flex min-h-225 py-8 lg:py-16 px-4 bg-center bg-cover bg-no-repeat"
  style={`--desktop-bg: url('${desktopImage.src}'); --mobile-bg: url('${mobileImage.src}')`}
>
  <div class:list={["flex flex-col lg:flex-row max-w-lg w-full mx-auto relative", { "gap-4": hasGap }]}>
    <slot name="right" />
    <slot name="left" />
  </div>
</section>

<style>
  section {
    background-image: var(--mobile-bg);
  }

  @media (min-width: 1200px) {
    section {
      background-image: var(--desktop-bg);
    }
  }
</style>
